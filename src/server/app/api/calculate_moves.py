import board.py


def pawn_calculate():




def calculate_possible_moves(chessboard): #takes in chessboard object
	board_list = chessboard.get_board()
	for column in range(len(board_list)):
		for tiles in range(len(board_list[column])):
			if board_list[

